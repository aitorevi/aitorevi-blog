---
interface Props {
  label: string;
  variant?: 'default' | 'accent' | 'muted' | 'crosspost' | 'raw';
  /** When provided, the tag renders as a link to this URL. */
  href?: string;
  class?: string;
}

const { label, variant = 'default', href, class: className = '' } = Astro.props;

const variantStyles = {
  default: 'bg-grayLight text-quaternary',
  accent: 'bg-accent/10 text-accent border border-accent/20',
  muted: 'bg-slate-50 text-slate-700 border border-slate-200 dark:bg-white/[0.03] dark:text-slate-400 dark:border-white/10',
  crosspost: 'bg-slate-100 text-slate-600 border border-slate-300 dark:bg-slate-800/40 dark:text-slate-400 dark:border-slate-600',
  raw: 'border',
};

const baseStyles = 'inline-flex w-fit items-center px-2.5 py-0.5 rounded-full font-mono text-[10px] 2xl:text-[12px] uppercase tracking-[0.15em]';
const interactive = href ? ' transition-colors duration-200 hover:text-accent hover:border-accent/50 cursor-pointer' : '';
const combinedClasses = `${baseStyles} ${variantStyles[variant]}${interactive} ${className}`;
---

{href ? (
  <a href={href} class={combinedClasses}>{label}</a>
) : (
  <span class={combinedClasses}>{label}</span>
)}
