---
import {EmailLinkToCopy} from "./EmailLinkToCopy";
const { position, copiedLabel = 'Copied', buttonLabel } = Astro.props;
---

<EmailLinkToCopy client:idle position={position} copiedLabel={copiedLabel} buttonLabel={buttonLabel}>
    <slot/>
</EmailLinkToCopy>