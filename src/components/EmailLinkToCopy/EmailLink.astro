---
import {EmailLinkToCopy} from "./EmailLinkToCopy";
const { position, copiedLabel = 'Copied' } = Astro.props;
---

<EmailLinkToCopy client:idle position={position} copiedLabel={copiedLabel}>
    <slot/>
</EmailLinkToCopy>